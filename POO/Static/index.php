<?php

namespace POO\Static;

require_once '../../vendor/autoload.php';

//dump(Booking::getCount());
dump(Booking::getInstance());
dump(Booking::getInstance());
dump(Booking::getInstance());
dump(Booking::getInstance());